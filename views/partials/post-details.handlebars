<section class="post">
    {{!-- make each post clickable ? and use that for routes/controllers --}}
    <a href="">
        <header>{{title}} by: {{user_id}}</header>
        <div class="post-body">
            <div>
                {{text}}
            </div>
        </div>
    </a>

</section>